import { StyleSheet, Text, View } from "react-native";

import React from "react";

const CheckoutAddressCard = () => {
  return (
    <View>
      <Text>CheckoutAddressCard</Text>
    </View>
  );
};

export default CheckoutAddressCard;

const styles = StyleSheet.create({});
